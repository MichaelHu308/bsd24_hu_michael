# Dokumentation
1. Maven installiert und Umgebungsvariablen gesetzt
2. Neuer Branch **init_project** erstellt
3. Neues Maven Projekt **bsd24_hu_michael** angelegt
4. Branch **init_project** auf das remote Repository gepusht
5. Neuer Branch **calculator** erstellt
6. Neue Java Klasse erstellt
7. Methoden für die calculator Klasse erstellt
8. Änderungen committed
9. Im pom.xml die **property** und **build** Blöcke eingefügt
10. Änderungen committed und auf remote Repository gepusht
11. Git pull request erstellt und freigegeben
12. Neuer branch **logging** erstellt
13. Neue Dependency für die Logging Bibliothek **log4j** in die pom.xml eingefügt
14. Neue Konfigurationsdatei **log4j2.xml** erstellt und konfiguriert
14. Alle Log Nachrichten in die Datei **myapp.log** umgeleitet
15. Änderungen committed, branch auf das remote Repository gepushed und git pull request erstellt/freigegeben
